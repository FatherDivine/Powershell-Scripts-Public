was found in ucdenver>ceas>LABS .
was supposed to set the BG, but the code in the .bat does not distinguish which lab is which
can make a PS that reads (& deciphers) hostname (split "-") and loads just the correct one. 
