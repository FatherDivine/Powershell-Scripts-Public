Plan:

Create a PS Script that sanitizes the hostname (split "-") and only copies the correct background to the correct lab, instead of all BG's to all pcs.

Could use FOG snap-in as well (to clear up Group Policy, apply faster, etc).

- Aaron S 6/28/23


The "COPY /V /Y EES.dft C:\EES32\" is "Engineering Equation Solver" and for ELEC (NC2609) Lab

